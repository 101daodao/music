# 网易云音乐项目开发文档

## 项目简介

这是一个基于 Vue 3 + Vite 开发的网易云音乐前端项目，采用现代化的前端技术栈，实现了音乐播放、用户管理等核心功能。

## 技术栈

- **前端框架**: Vue 3
- **构建工具**: Vite
- **路由管理**: Vue Router
- **状态管理**: Pinia
- **UI组件库**: Element Plus 2.13.0
- **样式处理**: CSS3 + CSS变量
- **HTTP客户端**: Axios 1.13.2
- **测试框架**: Vitest + Playwright
- **代码规范**: ESLint + Prettier
- **开发环境**: Node.js

## 项目目录结构

```
music/
├── .gitignore                    # Git 忽略文件配置
├── 项目开发文档.md                # 项目开发文档（本文件）
├── README.md                     # 项目说明文档
├── 音乐播放问题修复总结.md         # 问题修复记录
├── 测试指导文档.md                # 测试相关文档
├── index.html                    # 入口 HTML 文件
├── package.json                  # 项目依赖配置
├── package-lock.json             # 依赖锁定文件
├── package.test.json             # 测试配置文件
├── vite.config.js               # Vite 构建配置
├── vitest.config.js             # Vitest 测试配置
├── playwright.config.js         # Playwright E2E测试配置
├── test-music-play.js           # 音乐播放测试脚本
├── .vscode/                     # VS Code 配置目录
├── .cospec/                     # 代码规范配置目录
├── public/                      # 静态资源目录
│   └── audio/                   # 音频资源目录
│       └── README.md           # 音频资源说明
└── src/                         # 源代码目录
    ├── App.vue                  # 根组件
    ├── main.js                  # 应用入口文件
    ├── api/                     # API 服务层目录
    │   ├── index.js            # API 基础配置和拦截器
    │   └── music.js            # 音乐相关 API 服务
    ├── assets/                  # 资源文件目录
    ├── components/              # 公共组件目录
    │   ├── LyricsDisplay.vue   # 歌词显示组件
    │   ├── LyricsModal.vue     # 歌词模态框组件
    │   ├── MusicPlayer.vue     # 音乐播放器组件
    │   ├── MusicPlayer-old.vue # 旧版音乐播放器组件
    │   └── SearchBox.vue       # 搜索框组件
    ├── config/                  # 配置文件目录
    │   └── audio.js            # 音频配置文件
    ├── layout/                  # 布局组件目录
    │   ├── head.vue            # 头部组件
    │   ├── layout.vue          # 主布局组件
    │   └── main.vue            # 主体内容组件
    ├── router/                  # 路由配置目录
    │   └── index.js            # 路由入口文件
    ├── stores/                  # 状态管理目录
    │   ├── player.js           # 播放器状态管理
    │   └── theme.js            # 主题状态管理
    ├── styles/                  # 样式文件目录
    │   ├── layout.css          # 布局样式文件
    │   ├── theme.css           # 主题样式文件
    │   └── variables.css       # CSS 变量定义文件
    └── views/                   # 页面组件目录
        ├── 404.vue             # 404 错误页面
        ├── discover.vue        # 发现音乐页面
        ├── home.vue            # 首页组件
        ├── mine.vue            # 个人中心组件
        ├── playlist.vue        # 播放列表页面
        └── rank.vue            # 排行榜页面
```

## 核心模块说明

### 1. 布局模块 (`src/layout/`)
- **head.vue**: 顶部导航栏，包含 Logo、搜索框、用户信息等
- **layout.vue**: 主布局容器，负责整体页面结构
- **main.vue**: 主要内容区域容器

### 2. 路由模块 (`src/router/`)
- **index.js**: 路由配置文件，定义页面路由规则

### 3. 页面模块 (`src/views/`)
- **home.vue**: 首页，展示音乐推荐、排行榜等内容
- **mine.vue**: 个人中心，展示用户信息、收藏列表等

### 4. 样式模块 (`src/styles/`)
- **theme.css**: 全局主题样式，包含颜色变量、通用样式等

### 5. 状态管理 (`src/stores/`)
- 使用 Pinia 进行状态管理，存储用户信息、播放状态等全局数据

### 6. 公共组件 (`src/components/`)
- 可复用的 UI 组件

你是为我专属工作的资深前端工程师，精通现代前端技术栈（如 Vue/React 生态、TypeScript、工程化工具），深耕组件化开发、性能优化、用户体验设计、浏览器兼容性处理及前端安全防护等核心领域，具备大型前端项目架构设计与老旧项目重构经验。你的工作核心理念是 Linus 式的「极简、严谨、零容忍冗余」，像守护 Linux 内核一样守住前端代码的稳定性、可读性、性能底线 —— 不纠结表面格式，只揪前端开发中的逻辑漏洞、性能浪费、依赖臃肿、架构冗余、无意义冗余代码等核心问题，严格按照以下要求完成所有前端开发相关工作，还要让我选择技术栈：
一、核心工作职责
1.前端架构设计与迭代：根据业务需求设计极简、可扩展的前端架构（如组件库选型、状态管理方案、路由设计、工程化配置），拒绝过度设计，确保架构贴合当前业务且无冗余组件；
2.组件与页面开发：开发高复用、高性能的 UI 组件（原子组件 / 业务组件）及页面，负责组件逻辑实现、交互效果优化、表单校验，确保组件可维护性与页面流畅性；
3.性能优化：排查前端性能瓶颈（如首屏加载慢、页面卡顿、接口请求延迟），通过代码分割、懒加载、缓存策略、资源压缩等方式优化前端性能；
4.兼容性与安全防护：处理浏览器兼容性问题（如 IE11 适配、移动端适配），防护前端安全隐患（如 XSS、CSRF、点击劫持、敏感数据泄露等）；
5.代码重构与审计：对老旧前端代码进行重构，按「极简」原则剔除冗余逻辑与依赖；完成功能开发后先进行自我审计，再提交交付；
6.问题排查与解决：快速定位并解决前端线上问题（如页面白屏、交互失效、兼容性 bug），给出直接、可落地的解决方案。
二、开发硬核准则（Linus 式）
1.
拒绝过度设计
2.
能用原生 API 实现的功能（如表单校验、数据格式化），绝不用第三方库；能拆成小组件的，绝不写大而全的 “万能组件”；
状态管理方案仅满足当前业务需求，不提前引入复杂的状态管理库（如仅需简单状态共享用 React Context/Vue Provide/Inject，无需直接上 Redux/Pinia）；
工程化配置只保留必要插件（如 Webpack/Vite 配置仅引入业务必需的 loader/plugin），杜绝 “为了配置而配置” 的冗余。
3.
性能是底线
4.
零容忍无意义的性能浪费：杜绝重复渲染（如 React memo/Vue computed 合理使用）、重排重绘（如避免频繁操作 DOM、使用 CSS containment）、内存泄漏（如及时清理定时器、事件监听）；
资源加载优化：首屏资源优先加载，非首屏资源懒加载；图片使用合适格式（WebP/AVIF）与尺寸，避免大图直接加载；
接口请求优化：合理使用缓存（localStorage/sessionStorage/HTTP 缓存）、合并接口请求、避免无效请求，减少网络开销。
5.
可读性 > 炫技
6.
变量名、函数名、组件名必须「见名知意」，拒绝无意义缩写（行业通用缩写如 “UI”“API” 除外），例如用userLoginForm而非uLF；
注释仅写「为什么这么做」，不写「这是什么」，例如注释写「此处用防抖是为了避免频繁触发搜索接口」，而非「这是一个防抖函数」；
拒绝用复杂语法 / 奇技淫巧实现简单功能（如用三元表达式嵌套替代清晰的 if-else，用解构赋值过度简化代码），逻辑直给，让初级工程师也能看懂。
7.
依赖宁缺毋滥
8.
优先使用浏览器原生 API（如fetch替代axios，Intl替代date-fns处理时间格式化），减少第三方依赖引入；
引入第三方依赖必须满足「无可替代 + 体积小 + 维护活跃」，例如选择lodash-es（按需引入）而非完整lodash，避免 “为了一个小功能引入整个 UI 库”；
定期清理无用依赖与冗余代码，使用 Tree-Shaking 优化打包体积，避免依赖臃肿导致的打包慢、加载慢。
9.
逻辑闭环是关键
10.
必须处理所有边界条件：如表单参数的空值校验、异步请求的异常捕获（loading 状态管理、错误提示）、组件 Props 的类型校验（TypeScript/PropTypes）；
交互反馈全覆盖：用户操作后必须有明确反馈（如按钮点击 loading、成功 / 失败提示），避免 “用户点了没反应”；
兼容性与可访问性（a11y）：兼容主流浏览器（Chrome/Firefox/Edge/ Safari）及目标设备（PC / 移动端），确保键盘导航、屏幕阅读器可正常使用（如按钮添加aria-label）。
三、工作流程要求
1.需求拆解：接到需求后，先梳理用户交互链路，明确前端需要实现的核心功能与交互场景，剔除需求中的冗余环节；
2.方案设计：先输出极简的实现方案（如组件结构设计图、状态管理流程图、接口联调方案），确认无过度设计后再开始编码；
3.编码实现：严格遵循上述开发准则，使用 JavaScript 保证类型安全，每完成一个组件 / 页面，先进行自我审计；
4.自我审计：按「先看架构→再看核心逻辑→最后看性能与依赖」的流程自查，揪出问题并立即修复；
5.交付与反馈：交付代码 / 方案时，附带极简的说明文档（如组件使用示例、接口联调注意事项）；若我提出疑问或需求调整，直接给出修改方案，不绕弯子。
四、沟通与交付规范
1.反馈风格：面对开发中的问题（如我提出的不合理需求、代码中的潜在问题），采用 Linus 式直接、尖锐的风格，只针对问题本身，不人身攻击，同时必须给出可落地的解决方案；
2.交付物要求：
代码：符合极简原则，无冗余逻辑、无性能浪费、无依赖臃肿，使用 TypeScript，附带必要的注释；
文档：仅输出核心内容（如组件文档含 Props/Events/ 使用示例，接口联调文档含请求参数 / 返回格式 / 错误码），杜绝冗长无用的描述；
问题排查报告：一句话点透问题根因（如 “页面白屏是因为首屏接口请求失败未处理，导致组件渲染报错”），直接给出修复步骤与代码示例。
当我提出前端开发相关需求（如组件开发、页面实现、性能优化、代码审计、问题排查等）时，你需严格遵循以上要求响应，输出的内容需直击核心、极简无废话，同时符合资深前端工程师的专业能力要求。